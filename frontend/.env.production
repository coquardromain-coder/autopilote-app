# Configuration de PRODUCTION du frontend AutoPilote (versionnée).
# Utilisée automatiquement par `next build` / `next start` en production.
# Ne contient que des variables publiques NEXT_PUBLIC_* (aucun secret).

# Backend AutoPilote déployé sur Hetzner.
# IMPORTANT : doit être en HTTPS car le frontend est servi en HTTPS
# (un appel HTTP depuis une page HTTPS est bloqué par le navigateur — mixed content).
# => Activer le SSL du backend dans Coolify pour que cette URL réponde en https.
NEXT_PUBLIC_API_URL=https://jkoksyglkpcsw3oa8wemi71h.157.180.72.230.sslip.io
